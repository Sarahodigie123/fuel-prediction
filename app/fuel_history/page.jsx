//import React from 'react';
import FuelHistory from "../components/fuel_history"; // Make sure the component name matches the default export of the component file

export default function FuelHistoryPage() {
  return (
    <main>
      <FuelHistory /> // This should match the name of the imported component
    </main>
  );
}
